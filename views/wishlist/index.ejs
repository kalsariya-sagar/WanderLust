<% layout("/layouts/boilerplate") %>

<div class="d-flex justify-content-between align-items-center mb-4 page-header">
    <h2 class="fw-bold mb-0 page-title">
        <i class="fa-solid fa-heart text-danger me-2"></i> My Wishlist
    </h2>
</div>

<% if (!wishlist || wishlist.length === 0) { %>

    <div class="text-center py-5 my-4">
        <div class="mb-3">
            <i class="fa-regular fa-heart fa-4x text-muted opacity-50"></i>
        </div>
        <h4 class="fw-bold mb-2">Your wishlist is empty</h4>
        <p class="text-muted mb-4">Save your favorite listings to easily find them later.</p>
        <a href="/listings" class="btn btn-primary-custom px-4 py-2 rounded-3 fw-semibold">
            Explore Listings
        </a>
    </div>

<% } else { %>

    <div class="row g-4">
        <% wishlist.forEach((listing) => { %>
            <div class="col-12 col-sm-6 col-lg-4 col-xl-3">
                <div class="card listing-card h-100 border-0">
                    
                    <!-- Wishlist Remove Button -->
                    <div class="wishlist-btn-wrapper">
                        <form action="/wishlist/<%= listing._id %>?_method=DELETE" method="POST">
                            <button type="submit" class="wishlist-icon active" aria-label="Remove from wishlist">
                                <i class="fa-solid fa-heart"></i>
                            </button>
                        </form>
                    </div>

                    <!-- Listing Details Link -->
                    <a href="/listings/<%= listing._id %>" class="listing-link">
                        <div class="img-container">
                            <img 
                                src="<%= listing.image.url %>" 
                                alt="<%= listing.title %>" 
                                class="card-img-top listing-img"
                                loading="lazy"
                            >
                        </div>

                        <div class="card-body px-1 py-2">
                            <h5 class="card-title text-truncate fw-bold mb-1">
                                <%= listing.title %>
                            </h5>

                            <p class="card-text text-muted small mb-1">
                                <i class="fa-solid fa-location-dot me-1 text-danger"></i>
                                <%= listing.location %>, <%= listing.country %>
                            </p>

                            <p class="card-text mb-0 mt-2">
                                <span class="listing-price fw-bold text-dark fs-6" data-price="<%= listing.price %>">
                                    ₹ <%= listing.price ? listing.price.toLocaleString("en-IN") : 'N/A' %>
                                </span>
                                <span class="text-muted small">/ night</span>
                            </p>
                        </div>
                    </a>

                </div>
            </div>
        <% }) %>
    </div>

<% } %>